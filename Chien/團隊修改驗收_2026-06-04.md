# 團隊修改驗收報告（main 已整合）

> 驗收日期：2026-06-04
> 範圍：pull 最新 main 後，逐檔比對 [Chien_專案全面檢視_問題清單.md](../Chien_專案全面檢視_問題清單.md) 與 [post-sync-review.md](../post-sync-review.md) 的每一項
> 方法：直接讀 main 上的實際程式碼（不只看文件打勾）

---

## 一、三人進度總覽

| 負責人 | 項目 | 文件標記 | 實際程式碼 | 結論 |
|--------|------|----------|-----------|------|
| 蔡晟郁 | A1 座位 fare_class 讀 coach 層 | ✅ | `seed_postgres.py:226` `coach.get("fare_class")` | ✅ 真的修了 |
| 蔡晟郁 | A2 login_user 補 first_name/surname | ✅ | `queries.py:674-677` 由 full_name 拆分 | ✅ |
| 蔡晟郁 | A3 HNSW index 命名 | ✅ | `schema.sql:279` `idx_policy_documents_embedding` | ✅ |
| 蔡晟郁 | A4 year_of_birth | ✅ | `queries.py:300-302` | ✅ |
| 蔡晟郁 | A5 FK ON DELETE + comment | ✅ | `schema.sql` 16 條 ON DELETE（RESTRICT/SET NULL/CASCADE） | ✅ |
| **Chien** | C1 graph schema 重寫 | — | `queries.py` / `seed_neo4j.py` / `seed.cypher` 全部新 schema | ✅ 已併入 main（PR #22）|
| **Chien** | C2 cheapest 用邊票價屬性 | — | `query_cheapest_route` 用 `_infer_network` + dijkstra 權重 | ✅ |
| **Chien** | C3 station_connections / driver / 註解 | — | per-call driver、無 `r.network`、WHY 註解齊 | ✅ |
| 蔣耀德 | embed() module-level cache | （post-sync 🟡）| `llm_provider.py:28,145-149` `_embed_cache` | ✅ 真的補了 |
| 蔣耀德 | **J1 reranker 接進 pipeline** | 🟠 待處理 | `agent.py:386` 仍直呼 `query_policy_vector_search` | ❌ **尚未做** |
| 蔣耀德 | J2 embedding 維度/門檻驗證 | 🟡 | 需實跑環境才能驗 | ⏳ 待環境 |

**一句話**：蔡晟郁 + Chien 都已完成並進 main；蔣耀德只完成 embed cache，**J1（reranker 串接）還沒做**。

---

## 二、本次新發現的問題（不在原清單，請補進待辦）

### 🟠 D1 — `AI_SESSION_CONTEXT.md` 的 graph schema 整段過時，與 main 程式碼相矛盾
**檔案**：[AI_SESSION_CONTEXT.md:231-278](../AI_SESSION_CONTEXT.md#L231-L278) 與 [AI_SESSION_CONTEXT.md:329](../AI_SESSION_CONTEXT.md#L329)

這是 post-sync-review「黃改好 graph schema 後，蔡同步更新 AI_SESSION_CONTEXT.md」(Q11) 那一項——**還沒做**，而且現在問題更嚴重：Chien 的新 schema 已經進 main，但這份「schema 契約文件」仍寫舊的：

| 文件現況（錯） | main 程式碼（對） |
|----------------|-------------------|
| `Status: CONFIRMED — Q1 single Station label ✓`（234 行） | 已改 `MetroStation` / `NationalRailStation` |
| `CONNECTS_TO {line, travel_time_min, network}`（249-254 行） | 已改 `METRO_LINK` / `RAIL_LINK`（含 `fare_usd`/`fare_standard_usd`/`fare_first_usd`）|
| `INTERCHANGE_WITH`（256-257 行）| 已改 `INTERCHANGE_TO {transfer_time_min:5}` |
| 「No split into MetroStation/NationalRailStation」（245 行） | 正好相反，已 split |
| 329 行 changelog：「single Station label, CONNECTS_TO… 66 edges」 | 與實際拓撲不符 |

**為何要修**：這份文件是給「閱卷 TA + 寫 Design Document 的人」看的 schema 契約。文件說 A、程式碼做 B → Design Doc Section 3（Graph Rationale /25）會自相矛盾、扣分；組員寫報告時也會抄到錯的 schema。
**負責**：Q11 已定 = Chien 出新 schema 文字、蔡晟郁貼進去。**Chien 的部分（草稿文字）我可以直接幫你產**，產完轉給蔡替換 231-278 行那段 + 329 行 changelog。

---

### 🟡 D2 — Chien graph 檔案殘留兩行無害死碼
1. [skeleton/seed_neo4j.py:97](../skeleton/seed_neo4j.py#L97)
   ```python
   t = s["travel_time_min"] if "travel_time_min" in s else None   # 下一行迴圈內立刻被覆蓋，metro JSON 也無此頂層欄位
   ```
2. [databases/graph/queries.py:149](../databases/graph/queries.py#L149)
   ```python
   if net == "rail":          # 走到這裡時 net 只可能是 metro/national_rail/unknown，永遠不會是 "rail"
       net = "national_rail"
   ```
   功能正確（靠 origin_id 推斷），這行只是到不了的防禦碼。

**影響**：純美觀，不影響功能或評分。graded 檔案建議清掉，要的話我一次幫你刪（屬 Chien 檔案範圍，可自行處理）。

---

### 🟡 D3 — 聊天路徑的 cheapest route 不會帶 fare_class（僅備忘，**不要改 agent.py**）
[skeleton/agent.py:411-416](../skeleton/agent.py#L411-L416) 的 `find_route` 呼叫 `query_cheapest_route` 時**沒傳 `fare_class`** → 透過聊天問「最便宜」一律當 `standard`。
- Live C2 評分是**直接呼叫** `query_cheapest_route(..., fare_class="first")`，函式本身已正確支援 → **不失分**。
- `agent.py` 標示「不需修改」，所以**不動它**。只是 Live demo 時若要展示 first class 差異，請用直接呼叫函式那條，而非聊天。

---

## 三、仍 OPEN 的既有項目

| 項目 | 狀態 | 說明 |
|------|------|------|
| **J1** reranker 串接（蔣）| ❌ | `rag.search_with_rerank` 只有 `rag.py` 自己引用，agent 沒接 → cross-encoder 重排在聊天/Live 完全不會跑。需團隊決定：(a) 動 `agent.py` 把 `search_policy` 改用 `search_with_rerank`；或 (b) 不動 agent，準備 `scratch/test_rerank.py` 在 Live 直接 demo 重排前後差異。 |
| J2 維度/門檻（蔣）| ⏳ | 等環境跑 `seed_vectors.py` 後，用幾個 query 驗 `query_policy_vector_search` 真有回東西，必要時調低 `VECTOR_SIMILARITY_THRESHOLD`。 |
| A5 junction table（蔡）| 🟡 | `stops_in_order VARCHAR[]` 是否改 junction table，仍是「待討論」的設計取捨；若不改，要在 Design Doc Section 2 寫明選陣列的理由。 |
| 環境跑通（三人）| ❌ | post-sync §七 Steps 2–12 全未做，至少一人要跑通全套 seed + live。 |
| 繳交文件（三人）| ❌ | Design Document / Work Allocation / Peer Review 都未開始。 |

---

## 四、結論

- 🟢 **Relational（蔡）完成**、**Graph（Chien）完成並進 main**。
- 🟠 **Vector（蔣）J1 未做**，是目前唯一還卡著的「程式碼」項目。
- 🟠 **D1：AI_SESSION_CONTEXT.md graph 段落過時**是本次最該優先補的新問題（文件 ↔ 程式碼不一致）。
- 🟡 D2/D3 為美觀/備忘，不影響評分。