# 專案現況總覽（更新至 2026-06-05）

> 由 Claude 驗收生成。查核對象：main 實際程式碼，非文件 checkmark。

---

## 一、最新 PR 合併記錄

| PR | 作者 | 標題 | 狀態 |
|----|------|------|------|
| #22 | Chien | feat(graph): rewrite Neo4j schema with MetroStation/NationalRailStation | ✅ Merged |
| #25 | Chien | fix: update AI_SESSION_CONTEXT graph schema + restore llm_provider | ✅ Merged |
| #26 | 蔣耀德 | Feature/rag pipeline fix (reranker score) | ✅ Merged |
| #27 | 蔡晟郁 | fix: correct cypher syntax in query_delay_ripple and clean imports | ✅ Merged |
| #28 | 蔣耀德 | feat(rag): add embedding cache and hook reranker to agent.py | ✅ Merged |
| #29 | 蔡晟郁 | docs: live testing feedback and postgres seeding fix | ✅ Merged |
| #30 | Chien | fix(graph): resolve C3 duplicate routes and C4 interchange timeout | 🟡 Open（待 merge） |

main 目前 HEAD：`02c6307`（PR #29 merge 後）。本地與 origin/main 同步。

---

## 二、各問題最終狀態

### Chien 負責（Graph DB）

| ID | 問題 | 程式碼狀態 |
|----|------|-----------|
| C1 | Neo4j split-label schema（MetroStation/NationalRailStation） | ✅ 已完成 (`databases/graph/queries.py`) |
| C2 | APOC dijkstra 使用 `'METRO_LINK\|RAIL_LINK'` relType | ✅ 正確 |
| C3 | 票價在 seed 時寫進 edge（Q5=A） | ✅ 正確 (`skeleton/seed_neo4j.py`) |
| N1 | `_infer_network()` 用在 `query_cheapest_route`（避免 `"auto"` 對比失敗） | ✅ 已修 |
| N2 | `query_interchange_path` 回傳鍵為 `path`/`interchange_points` | ✅ 已修 |
| dead code | `seed_neo4j.py` 多餘的 `t = s["travel_time_min"]`；`queries.py` 死碼 `if net == "rail"` | ✅ 已刪 (PR #25) |
| C5 Cypher bug | `query_delay_ripple` 裡 `min(length(shortestPath(...)))` 語法非法 | ✅ 蔡已修 (PR #27)，改用具名 path |
| C3_dup | `query_alternative_routes` 重複路線（無 DISTINCT） | ✅ 已修 (PR #30)，加 `WITH` + `RETURN DISTINCT` |
| C4_timeout | `query_interchange_path` `*1..20` 超時 | ✅ 已修 (PR #30)，改用 `shortestPath(*1..10)` |

### 蔡晟郁負責（Relational DB）

| ID | 問題 | 程式碼狀態 |
|----|------|-----------|
| A1 | `seed_postgres.py` `seed_seat_layouts` fare_class 讀錯層級 | ✅ 已修 (PR #29) |
| A2 | `login_user` 缺 `first_name`/`surname` 拆分 → UI KeyError crash | ✅ 已修 (PR #29) |
| A3 | `query_user_profile` 缺 `year_of_birth` | ✅ 已修 (PR #29) |
| A4 | CHECK 約束、FK ON DELETE、soft delete comment | ✅ 已確認 |
| A5 | `seed_metro_travels` `stops_travelled` null → IntegrityError | ✅ 已修 (PR #29) |
| A6 | `stops_in_order VARCHAR[]` vs junction table | ⚠️ 保留陣列，**需在 Design Doc §2 說明理由** |

### 蔣耀德負責（RAG / Vector DB）

| ID | 問題 | 程式碼狀態 |
|----|------|-----------|
| J1 | `search_with_rerank` 串接進 `agent.py` | ✅ **PR #28 已修**：`agent.py:386` 呼叫 `search_with_rerank(embedding, params["query"])` |
| J2 | embedding 維度 / threshold 驗證 | ✅ 蔡實測：RAG 回傳 5 筆 similarity 0.607–0.664，reranker 正常（PR #29 紀錄） |

---

## 三、剩餘程式碼問題

### `scratch/test_rerank.py` Part 1 Cache Demo 會印出 ❌（低優先，demo 用）

```python
v1 = llm.embed("退票政策")
v2 = llm.embed("退票政策")
if v1 is v2:
    print("✅ Cache hit!")   # llm_provider 無 cache → 永遠不走這行
else:
    print("❌ Cache miss!")  # Live Testing 時評審會看到這行
```

**建議**：刪掉 Part 1（第 14–21 行），只留 Part 2 rerank demo。非阻斷性問題，但 Live Testing 跑 demo 時會出現 ❌。

---

## 四、main 程式碼全面驗收結論

| 模組 | 狀態 | 備註 |
|------|------|------|
| `databases/graph/queries.py` | ✅ 全部正確（PR #30 後） | 6 函式正確；C3/C4/C5 全修；N1/N2 修正 |
| `skeleton/seed_neo4j.py` | ✅ 正確 | MERGE 去重、雙向 INTERCHANGE_TO、票價公式正確 |
| `databases/graph/seed.cypher` | ✅ 正確 | 與 .py 一致 |
| `skeleton/seed_postgres.py` | ✅ 已修 (PR #29) | fare_class 讀 coach 層、stops_travelled null→0 |
| `skeleton/agent.py` | ✅ J1 已修 | `search_with_rerank` 串接完成 |
| `skeleton/rag.py` | ✅ 正確 | Top-20 vector + cross-encoder rerank |
| `skeleton/reranker.py` | ✅ 正確 | `rerank_score` 掛在結果上 |
| `skeleton/llm_provider.py` | ✅ 回到 upstream | 無多餘修改 |
| `AI_SESSION_CONTEXT.md` | ✅ 已更新 | 新 graph schema 對齊 main 程式碼 |
| `AI_SESSION_CONTEXT.zh-TW.md` | ✅ 已更新 | 同上，中文版 |
| `scratch/test_rerank.py` | ⚠️ Part 1 會印 ❌ | 建議刪掉 Part 1，只留 Part 2 |

---

## 五、接下來要做的事（按優先順序）

### 立即

- [ ] **Merge PR #30**（C3/C4 graph 修正）
- [ ] **修 `test_rerank.py` Part 1**：刪掉 cache 測試段，避免 Live Testing 印出 ❌

### 環境

蔡已在 2026-06-05 跑通完整環境（詳見 `live-testing-notes.md`）：

- [x] docker compose up（PostgreSQL + Neo4j 均 healthy）
- [x] seed_postgres.py（修正後通過）
- [x] seed_vectors.py（101 chunks 存入）
- [x] seed_neo4j.py（20 MetroStation, 10 NationalRailStation, 66 edges）
- [x] ui.py（http://127.0.0.1:7860 正常開啟）

**Chien / 蔣 各自環境仍需自行建立**（venv → .env → docker → seed × 3 → ui.py）。

### 文件交付（期末繳交）

| 文件 | 負責 | 狀態 |
|------|------|------|
| Design Document §1+2（Relational DB） | 蔡晟郁 | ❌ 未開始 |
| Design Document §3（Graph DB） | Chien | ❌ 未開始（草稿在 `Chien/D1_graph_schema_給蔡更新AI_SESSION_CONTEXT.md`） |
| Design Document §4（RAG / Vector DB） | 蔣耀德 | ❌ 未開始 |
| Design Document §5+6（系統整合 / 測試） | 三人合作 | ❌ 未開始 |
| Work Allocation Report（`WORK_ALLOCATION_TEMPLATE.md`） | 三人 | ❌ 未填 |
| Peer Review（`PEER_REVIEW_TEMPLATE.md`） | 各自填 | ❌ 未填 |

### Live Testing 排演

蔡已測試大部分函式（詳見 `live-testing-notes.md`）。PR #30 merge 後需重測 C3/C4：

- [ ] `query_interchange_path("MS01","NR05")` → 秒回、found=True、有 interchange_points
- [ ] `query_alternative_routes("MS01","MS09",avoid_station_id="MS07")` → 路線不重複
- [ ] `scratch/test_rerank.py` Part 2 demo 完整跑一遍

---

## 六、給 Chien 的 Design Doc §3 起點

草稿已在 [D1_graph_schema_給蔡更新AI_SESSION_CONTEXT.md](D1_graph_schema_給蔡更新AI_SESSION_CONTEXT.md)，中英兩版 schema 說明可直接用在 §3。

**§3 必寫重點**（評分標準）：
- nodes / relationships / properties 各自選型理由（不能只說「站是物件所以是 node」）
- **Dijkstra on graph vs SQL recursive CTE 具體演算法論證**（只說「graph 比較快」只得 20% 分數）
- `transfer_time_min=5` 是自訂合理值（規格未強制，教授確認可自訂）
- 兩種查詢類型（shortest path + delay ripple）及 graph model 如何讓它們可表達